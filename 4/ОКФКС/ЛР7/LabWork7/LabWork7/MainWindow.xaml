<Window x:Class="LabWork7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabWork7"
        mc:Ignorable="d"
        Title="Планировщик событий" Height="600" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Скрипт-редактор -->
        <TextBox x:Name="ScriptTextBox"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"
                 Text="Message(&quot;Привет из скрипта!&quot;);"
                 Height="100" />

        <!-- Список событий -->
        <DataGrid x:Name="EventsDataGrid"
                  Grid.Row="1"
                  Margin="0,10,0,0"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата и время" Binding="{Binding DateTime, StringFormat='{}{0:G}'}" Width="*"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Выполнить скрипт" x:Name="ExecuteButton" Click="ExecuteButton_Click" Padding="10,5" Margin="5"/>
            <Button Content="Загрузить скрипт из файла" x:Name="LoadScriptFromFile" Click="LoadScriptFromFile_Click" Padding="10,5" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
